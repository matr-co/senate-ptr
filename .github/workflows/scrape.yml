name: scrape
on:
  schedule:
    - cron: "17 */2 * * *"
  workflow_dispatch:
    inputs:
      since:
        description: "Backfill from YYYY-MM-DD (blank = last 14 days)"
        required: false
permissions:
  contents: write
concurrency: scrape
jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install requests
      - run: python scrape.py ${{ inputs.since && format('--since {0}', inputs.since) || '' }}
      - name: commit if changed
        run: |
          git config user.name "senate-ptr bot"
          git config user.email "actions@users.noreply.github.com"
          git add senate_ptrs.json
          git diff --cached --quiet || (git commit -m "update $(date -u +%FT%TZ)" && git push)
